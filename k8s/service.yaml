apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: learnhub
  labels:
    app: ecommerce
spec:
  type: NodePort
  selector:
    app: ecommerce
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30008
